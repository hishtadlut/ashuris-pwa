import { DoubleTapOpenImgDirective } from './double-tap-open-img.directive';

describe('DoubleTapOpenImgDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleTapOpenImgDirective();
    expect(directive).toBeTruthy();
  });
});
