import { CurserToEndDirective } from './curser-to-end.directive';

describe('CurserToEndDirective', () => {
  it('should create an instance', () => {
    const directive = new CurserToEndDirective();
    expect(directive).toBeTruthy();
  });
});
