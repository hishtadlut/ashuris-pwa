import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DealerListItemComponent } from './dealer-list-item.component';

describe('DealerListItemComponent', () => {
  // let component: DealerListItemComponent;
  // let fixture: ComponentFixture<DealerListItemComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ DealerListItemComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DealerListItemComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
