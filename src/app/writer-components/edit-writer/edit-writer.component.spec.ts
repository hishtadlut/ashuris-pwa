import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';
import { NgbModule } from '@ng-bootstrap/ng-bootstrap';
import { AudioHTML5Component } from 'src/app/audio-html5/audio-html5.component';
import { DoubleTapOpenImgDirective } from 'src/app/directives/double-tap-open-img/double-tap-open-img.directive';
import { HebrewDateFormControlComponent } from 'src/app/hebrew-date-form-control/hebrew-date-form-control.component';
import { NoteDialogComponent } from 'src/app/note-dialog/note-dialog.component';

import { EditWriterComponent } from './edit-writer.component';

describe('EditWriterComponent', () => {
  // let component: EditWriterComponent;
  // let fixture: ComponentFixture<EditWriterComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [ReactiveFormsModule, FormsModule, NgbModule],
  //     declarations: [ 
  //       EditWriterComponent, 
  //       NoteDialogComponent, 
  //       HebrewDateFormControlComponent, 
  //       AudioHTML5Component ,
  //       DoubleTapOpenImgDirective,
        
  //     ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(EditWriterComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
